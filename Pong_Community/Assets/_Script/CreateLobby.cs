using UnityEngine;
using System.Collections;

public class CreateLobby : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
	
	void OnGUI(){
		if(GUI.Button(new Rect(100, 100, 50, 50), "Create Lobby")){
			
		}
	}
}
